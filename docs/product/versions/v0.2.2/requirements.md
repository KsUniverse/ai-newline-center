# v0.2.2 需求文档 — 收藏同步 + 对标账号管理

> 版本: v0.2.2
> 里程碑: v0.2.x（账号管理）
> 需求来源: [PRD.md M-002](../../PRD.md) F-002-1 + F-002-2
> 创建日期: 2026-04-03

---

## 摘要

- **版本目标**: 通过定时爬取员工抖音收藏列表自动发现对标博主，并支持手动添加/归档对标账号
- **功能数量**: 2 个（F-002-1 收藏同步、F-002-2 对标账号管理）
- **优先级分布**: P0(2)
- **前置依赖**: v0.2.1.1（爬虫真实对接）已完成；DouyinAccount、DouyinVideo schema 已就绪
- **后续迭代**: v0.3.x 的 AI 拆解将以对标账号视频为素材入口

---

## 面向角色与访问权限表

| 角色 | F-002-1 收藏同步 | F-002-2 对标账号列表 | F-002-2 添加对标账号 | F-002-2 归档（软删除） |
|------|-----------------|---------------------|---------------------|----------------------|
| **员工** | 系统自动执行（无感知） | 查看**本组织全部**对标账号 | 可手动添加 | 仅可归档**自己创建**的账号 |
| **分公司负责人** | — | 查看**本分公司全部**对标账号 | 可手动添加 | 仅可归档**自己创建**的账号 |
| **超级管理员** | — | 查看**全平台全部**对标账号 | 可手动添加 | 仅可归档**自己创建**的账号 |

> **核心数据隔离规则**：对标账号是**全组织共享**的（所有员工可见），但归档权限仅限于创建者本人。这与"我的账号"（MY_ACCOUNT 按 userId 私有）形成区别。

---

## 功能清单

---

### F-002-1: 收藏同步 (P0)

**描述**: 系统每 5 分钟扫描所有员工的 `MY_ACCOUNT` 抖音账号收藏列表，自动发现新收藏视频，识别其所属博主，在全组织维度创建或关联对标账号（`BENCHMARK_ACCOUNT`）。

---

#### 触发机制

- **触发方式**: 全局定时任务，每 5 分钟执行一次
- **扫描对象**: 所有 `type=MY_ACCOUNT` 且 `secUserId != null` 且 `deletedAt IS NULL` 的账号
- **调度模式**: 与现有三个定时器保持一致，采用全局扫描模式（统一间隔轮询所有账号），不按账号独立注册定时器
- **独立定时器**: 本任务作为第四个定时器独立注册，运行间隔独立配置（默认 5 分钟）

---

#### 数据流

```
定时器触发
  → 查询所有 MY_ACCOUNT（secUserId 非空）
  → 对每个账号: 调用 fetchCollectionVideos(secUserId)
  → 时间窗口策略: 遍历收藏列表，遇到收藏时间 < (now - 1小时) 的记录则停止
  → 对每条收藏视频:
      → 提取所属博主的 secUserId（[PENDING-CRAWLER-DETAIL] 收藏列表返回格式中博主字段待确认）
      → 查 DB: 是否已存在此 secUserId 的 BENCHMARK_ACCOUNT
        ├── 不存在 → 调用 fetchUserProfile(secUserId) → 创建 DouyinAccount(type=BENCHMARK_ACCOUNT)
        └── 已存在（含已归档）→ 跳过
  → 新创建的 BENCHMARK_ACCOUNT 自动纳入 F-001-2 的视频同步定时器扫描范围（无需额外操作，定时器按 type 不限制）
```

---

#### 收藏同步策略（时间窗口）

| 参数 | 值 |
|------|-----|
| 触发频率 | 每 5 分钟 |
| 时间窗口 | 1 小时（可配置，默认 now - 1h） |
| 遍历方向 | 从最新收藏向历史方向遍历 |
| 停止条件 | 遇到收藏时间 < (now - 1小时) 的记录，立即停止（该条及之后不处理） |
| 去重策略 | **不用 videoId 去重**（多员工可能收藏同一视频）；用 secUserId 去重（同一博主只创建一个 BENCHMARK_ACCOUNT） |

**边界说明**：
- 「收藏时间」指该视频被员工收藏的时间（非视频发布时间）；该字段名从爬虫返回中读取，**[PENDING-CRAWLER-DETAIL] 字段名待 crawler.service.ts 实现阶段确认**
- 时间窗口覆盖的是"近 1 小时内有新收藏动作"的员工账号——系统每 5 分钟执行一次，每次向前看 1 小时，保证在网络或任务延迟时不漏数据
- 若同一员工多次收藏同一博主的不同视频，博主对标账号只创建一次

---

#### 对标账号自动创建规则

| 规则 | 说明 |
|------|-----|
| secUserId 全局唯一 | 同一 secUserId 只有一条 BENCHMARK_ACCOUNT，跨组织全局唯一 |
| 归属员工（userId） | 设定为**第一个触发该博主收藏的员工 ID**（first-creator 语义） |
| 归属组织（organizationId） | 取该员工的 organizationId |
| profileUrl | 从爬虫 fetchUserProfile 返回中提取，若无则构造 `https://www.douyin.com/user/{secUserId}` |
| 已归档账号处理 | 若同 secUserId 已有归档（`deletedAt != null`）的 BENCHMARK_ACCOUNT，不重复创建，亦不自动恢复；已归档账号对当前员工不可见 |
| 不递归触发 | BENCHMARK_ACCOUNT 不扫描其收藏列表 |

---

#### 权限

- 定时任务由系统自动执行，无需用户操作权限
- 创建出的对标账号对**本组织全体员工**可见（非仅创建者）

---

#### 边界与异常

- 爬虫调用失败（单个账号）：跳过该账号，记录错误日志，不中断整体扫描
- fetchCollectionVideos 返回空列表：跳过该账号，不报错
- 博主 secUserId 提取失败（[PENDING-CRAWLER-DETAIL]）：跳过该条视频，记录日志
- fetchUserProfile 失败：跳过该博主，不创建账号，记录日志

---

#### 验收标准

1. 当员工在抖音收藏了某博主的视频，且该收藏时间在 1 小时内，则 5 分钟内系统自动创建该博主的对标账号，出现在对标账号列表中
2. 同一博主被多个员工收藏时，对标账号只创建一次，userId 为最早触发创建的员工
3. 时间窗口外（收藏时间 > 1 小时前）的视频不触发新对标账号创建
4. 收藏同步任务单次账号爬虫失败不影响其他账号的处理继续执行
5. 对标账号创建后，F-001-2 的视频同步定时器下次执行时自动抓取该博主的视频列表

---

### F-002-2: 对标账号管理 (P0)

**描述**: 员工可在「对标账号」页面查看全组织的对标博主及其视频，支持手动添加对标账号、归档（软删除）对标账号、以及查看归档账号列表。

---

#### 列表展示

**入口**: 左侧导航「对标账号」→ `/benchmarks`

**主列表（未归档）**:
- 卡片 Grid 布局（与「我的账号」保持一致的视觉风格）
- 每张卡片展示：头像、昵称、粉丝数、作品数、创建者姓名
- 空状态：展示引导文字「还没有对标账号，可手动添加或通过抖音收藏自动同步」+ 添加按钮
- 支持分页（与现有视频列表逻辑一致）

**归档入口**:
- 在主列表页右上角或底部放置低调但可见的「查看已归档」文字链接（非按钮，避免干扰主操作）
- 点击进入归档列表页（`/benchmarks/archived`）

**单个账号详情**:
- 路径：`/benchmarks/[id]`
- 展示：博主信息卡（头像、昵称、粉丝数、简介、创建者）+ 视频列表
- 视频列表展示：封面、标题、发布时间、播放数、点赞数（与 MY_ACCOUNT 视频列表一致）
- 视频旁展示「已拆解」/「未拆解」状态标记（v0.2.2 阶段数据固定为「未拆解」，为 v0.3.x 占位）

---

#### 手动添加对标账号

**与 MY_ACCOUNT 添加流程完全一致，复用相同的 Drawer 组件**:

1. 点击「添加对标账号」按钮（主列表右上角）
2. 右侧 Drawer 滑出 → 输入抖音主页链接（支持 `https://www.douyin.com/user/xxxxx` 格式）
3. 点击「获取账号信息」→ 调用爬虫（getSecUserId → fetchUserProfile）→ 展示预览
4. 预览信息：头像、昵称、粉丝数、作品数、简介
5. 用户确认 → 点击「添加」→ 系统写入 `DouyinAccount`（`type=BENCHMARK_ACCOUNT`，`userId=当前登录用户ID`）→ Drawer 关闭 → 列表刷新

**手动添加的去重规则**:
- secUserId 全局唯一：若该 secUserId 已有**未归档**的 BENCHMARK_ACCOUNT，提示「该对标博主已存在」，不重复创建
- 若该 secUserId 已有**已归档**的 BENCHMARK_ACCOUNT，提示「该对标博主已被归档，请前往归档列表查看」，不重复创建
- profileUrl 全局唯一（同已有 MY_ACCOUNT 逻辑）：若与某 MY_ACCOUNT 的 profileUrl 相同，提示「该账号已作为我的账号被添加」

---

#### 手动归档（软删除）

**触发入口**:
- 对标账号卡片上的「···」菜单 → 「归档」选项
- 或对标账号详情页右上角「归档」按钮

**权限校验**:
- 仅允许 `userId == 当前登录用户 ID` 的账号才显示归档入口（其他人的账号不显示该操作）
- 后端双重校验：API 层验证 `douyinAccount.userId == session.userId`，不满足则返回 403

**操作确认**:
- 点击归档后弹出二次确认 Dialog：「归档后，该博主的账号和视频数据均保留但会从主列表隐藏。确认归档？」
- 确认 → 执行软删除（`deletedAt = now()`）→ 账号从主列表消失 → Toast 提示「已归档」

**归档效果**:
- `DouyinAccount.deletedAt` 设置为当前时间（软删除标准字段）
- `DouyinVideo` 数据**保留不变**（不修改 deletedAt）
- F-001-2 的视频同步定时器扫描时跳过已归档（`deletedAt != null`）的账号
- 收藏同步任务遇到同 secUserId 的博主时不重新创建（已归档则保持归档状态）

---

#### 归档列表查看

**入口**: 主列表「查看已归档」→ `/benchmarks/archived`

**展示**:
- 与主列表卡片格式一致，增加「归档时间」字段
- 归档账号详情页（`/benchmarks/archived/[id]`）可查看博主信息和视频列表（只读）
- 归档列表无归档/恢复操作（v0.2.2 不实现恢复功能）

**数据范围**:
- 查询条件：`type=BENCHMARK_ACCOUNT AND deletedAt IS NOT NULL`
- 权限同主列表（员工看本组织，分公司负责人看本公司，超管看全部）

---

#### 权限汇总

| 操作 | 員工 | 分公司负责人 | 超级管理员 |
|------|------|-------------|-----------|
| 查看主列表 | 本组织全部 | 本公司全部 | 全部 |
| 查看归档列表 | 本组织全部 | 本公司全部 | 全部 |
| 手动添加 | ✅ | ✅ | ✅ |
| 归档（软删除） | 仅自己创建的 | 仅自己创建的 | 仅自己创建的 |

---

#### 边界与异常

- 空状态：主列表无账号时展示引导；归档列表为空时展示「暂无归档账号」
- 手动添加爬虫失败：提示错误，保留 Drawer，允许重试，不写入数据库
- 已归档账号的视频列表：可查看但视频数据不再更新
- 删除对标账号（硬删除）：**v0.2.2 不提供**，仅支持归档（软删除）
- 分页：列表超过一定条数时支持分页（具体分页参数与现有列表保持一致）

---

#### 验收标准

1. 用户可通过「对标账号」页面查看本组织所有未归档的对标博主列表
2. 手动添加相同 secUserId 的对标账号时，系统给出明确提示并阻止重复创建
3. 员工只能归档自己创建（userId 匹配）的对标账号，他人账号无归档入口
4. 归档操作有二次确认弹窗，确认后账号从主列表消失，但数据保留
5. 主列表页有「查看已归档」入口，点击可进入归档列表
6. 归档账号的视频数据可在 `/benchmarks/archived/[id]` 中查看，数据不再更新
7. 当系统返回 403 时（非创建者尝试归档），前端展示权限不足提示

---

## 外部服务对接说明

### fetchCollectionVideos

**当前实现状态**（见 `src/server/services/crawler.service.ts`）：

```typescript
async fetchCollectionVideos(secUserId: string): Promise<CrawlerCollectionResult>
```

- **API 端点**: `GET /api/douyin/web/fetch_user_collection_videos`
- **请求参数**: `sec_user_id={secUserId}`
- **当前返回类型**: `CrawlerCollectionResult = Record<string, unknown>`（尚未结构化解析）
- **重试机制**: 与其他爬虫调用一致，最多重试 2 次，间隔 1s / 2s，超时 30s

**v0.2.2 需要补充实现的内容**（产品层面确认的业务字段，技术实现由架构师决策）：

| 业务字段 | 说明 | 爬虫字段名 |
|---------|------|-----------|
| 收藏时间 | 该视频被员工收藏的时间，用于时间窗口策略 | **[PENDING-CRAWLER-DETAIL]** 待实际调用确认，候选：`collect_time`、`create_time`、`favorited_at` |
| 视频所属博主 secUserId | 用于查找或创建 BENCHMARK_ACCOUNT | **[PENDING-CRAWLER-DETAIL]** 候选：`author.sec_user_id`、`author_info.sec_user_id` |
| awemeId | 视频唯一 ID（日志/调试用） | 预期字段：`aweme_id`（与现有 fetchVideoList 一致） |
| 分页/游标 | 收藏列表可能超过单页 | **[PENDING-CRAWLER-DETAIL]** 候选：`cursor`、`max_cursor`；是否支持分页待确认 |

> **[PENDING-CRAWLER-DETAIL] 处理建议**：架构师在实现 `fetchCollectionVideos` 时，按照现有 `fetchVideoList` 的 JSON key 探测模式（pickString / pickArray 等），实际调用一次爬虫记录完整 JSON 响应后再映射字段。

### 其他爬虫调用（复用现有实现）

| 场景 | 方法 | 说明 |
|------|------|------|
| 手动添加对标账号 Step 1 | `getSecUserId(url)` | 与 MY_ACCOUNT 添加完全一致 |
| 手动添加对标账号 Step 2 | `fetchUserProfile(secUserId)` | 获取博主基本信息 |
| 收藏同步中自动创建博主 | `fetchUserProfile(secUserId)` | 获取博主基本信息后写入 BENCHMARK_ACCOUNT |

