# v0.1.1 Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace v0.1.1 frontend mock data flows with real organization and user management API integration.

**Architecture:** Add a thin management client layer on top of the existing `apiClient`, then update the `/organizations` and `/users` client pages to fetch, mutate, and refresh real server data while preserving current UI components and permission guards. Keep backend contracts unchanged and rely on existing NextAuth session state for role-based branching.

**Tech Stack:** Next.js App Router, React client components, NextAuth, TypeScript, existing `/api` route handlers, Vitest

---

### Task 1: Add management API client helpers

**Files:**
- Create: `src/lib/management-client.ts`
- Test: `src/lib/management-client.test.ts`

- [ ] **Step 1: Write failing tests for organization and user API helper methods**
- [ ] **Step 2: Run the focused test file and confirm it fails for missing module**
- [ ] **Step 3: Implement minimal typed helper methods for list/create/update/status operations**
- [ ] **Step 4: Re-run the focused test file and confirm it passes**

### Task 2: Integrate organizations page with real API

**Files:**
- Modify: `src/app/(dashboard)/organizations/page.tsx`

- [ ] **Step 1: Replace mock initialization with real list loading**
- [ ] **Step 2: Wire create, update, and status toggle handlers to real endpoints**
- [ ] **Step 3: Keep loading and toast behavior aligned with API responses**

### Task 3: Integrate users page with real API

**Files:**
- Modify: `src/app/(dashboard)/users/page.tsx`

- [ ] **Step 1: Replace mock initialization and filtering with real list loading**
- [ ] **Step 2: Load organization options appropriately for SUPER_ADMIN and BRANCH_MANAGER**
- [ ] **Step 3: Wire create, update, and status toggle handlers to real endpoints**

### Task 4: Verify the integrated flow

**Files:**
- Modify: `src/components/features/organizations/organization-list.tsx`
- Modify: `src/components/features/users/user-list.tsx`
- Modify: `src/components/shared/layout/app-sidebar.tsx`

- [ ] **Step 1: Run targeted tests for the new management client**
- [ ] **Step 2: Run `pnpm lint`**
- [ ] **Step 3: Run `pnpm build`**
- [ ] **Step 4: Run a final manual API-backed smoke check where possible**
